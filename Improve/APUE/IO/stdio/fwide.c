//
// Created by 田明博 on 2024/5/30.
//
#include "stdio.h"
#include "wchar.h"
/*
 * int wide(FILE *fp, int mode);
 * 如果 mode 参数为正，它将流设置为宽字符模式，这意味着流将在字符模式下操作。
 * 如果 mode 参数为负，则将流设置为字节模式，这意味着流将在字节模式下操作。
 * 如果 mode 参数为0，它仅检查流的当前方向，但不做任何更改。
 * */
